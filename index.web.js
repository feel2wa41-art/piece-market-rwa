import {AppRegistry} from 'react-native';
import App from './App';

AppRegistry.registerComponent('PieceMarketRwa', () => App);
AppRegistry.runApplication('PieceMarketRwa', {
  rootTag: document.getElementById('root'),
});
